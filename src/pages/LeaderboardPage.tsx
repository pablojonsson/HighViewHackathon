const LeaderboardPage = () => (
  <div className="card stack">
    <h2>Leaderboard (mock)</h2>
    <p className="subtle">
      Placeholder for leaderboard content. Add charts or tables here when ready.
    </p>
  </div>
);

export default LeaderboardPage;
